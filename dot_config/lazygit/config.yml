git:
  branchLogCmd: "git log --graph --color=always --abbrev-commit --decorate --date=relative --pretty=medium --oneline {{branchName}} --"
  paging:
    colorArg: always
    pager: delta --dark --paging=never

services:
  'git.ridewithvia.dev': 'gitlab:git.ridewithvia.dev'

gui:
  showIcons: true

